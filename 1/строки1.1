def search_substr(subst, st):
    if subst.lower() in st.lower():
        	return 'Есть контакт!'
    else:
    	return 'Мимо!'

print(search_substr('Мол', 'моЛокОл'))
print(search_substr('Молобок', 'МолобоК'))
print(search_substr('КМол', 'Труд'))
